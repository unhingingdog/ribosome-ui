# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
synopsis:
  "Typed template ADT, codec, validation, and reconciliation for generative UI"
description:
  "Ribosome owns a closed OCaml template ADT plus JSON codecs, invariant validation, stable-ID reconciliation, session state, and mode/skill bundles. It has no dependency on MCP, Dream, WebSockets, or any harness."
depends: [
  "dune" {>= "3.18"}
  "ocaml" {>= "5.2"}
  "telomere"
  "yojson"
  "alcotest" {with-test & >= "1.8.0"}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
x-maintenance-intent: ["(latest)"]
